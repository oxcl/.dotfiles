(provide 'features/builtins)

;; configuration for emacs builtin packages
;; builtin packages that have a lot of configuration and heavy modifications
;; like org-mode and cua-mode have their own feature/package modules.
;; this is mainly for builtin packages with small configurations

(require 'packages/dired)


