#!/usr/bin/env bash
[[ ! -d $HOME/.config ]] && mkdir -p $HOME/.config
[[ ! -d $HOME/.local ]] && mkdir -p $HOME/.local
[[ ! -d $HOME/.config/chromium ]] && mkdir -p $HOME/.config/chromium
(cd $HOME/.dotfiles/home && stow . --target=$HOME)
